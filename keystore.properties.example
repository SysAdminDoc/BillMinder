# Copy this file to keystore.properties in the repository root and fill in your own values.
# keystore.properties is never committed. Release builds fail with a clear message without it.

# Path to the signing keystore, relative to the repository root or absolute.
storeFile=billminder.jks
storePassword=
keyAlias=billminder
keyPassword=
