package com.sysadmindoc.billminder.wear

import android.content.Context

object WearSync {
    fun sync(context: Context) {
        // Wear Data Layer is intentionally absent from the F-Droid build.
    }
}
