<?xml version="1.0" encoding="utf-8"?>
<!--
    Permissions the open-source build ships that the Play build does not. Play restricts each of
    these to app categories BillMinder does not claim eligibility for, so they are declared here
    rather than in the shared manifest.

    USE_EXACT_ALARM      auto-grants exact alarms; Play limits it to alarm clock and calendar apps.
    USE_FULL_SCREEN_INTENT  drives the opt-in alarm-style due screen; Play limits it to calling and
                            alarm apps.
    READ_SMS             backs the opt-in local inbox scan; Play limits SMS access to default
                            handlers. The Play build reads a message the user shares instead.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.READ_SMS" />

</manifest>
